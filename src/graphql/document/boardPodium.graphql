query QBoardPodium($first: Int) {
  board(first: $first) {
    edges {
      node {
        rank
        moniker
        identity {
          kid
          picture {
            href
          }
        }
      }
    }
  }
}
